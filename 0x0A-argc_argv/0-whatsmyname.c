#include <stdio.h>

/**
 * main - prints the name od the program
 * @argc: argument count
 * @argv: argument value
 *
 * Return: always 0
 */

int main(int __attribute__((__unused__)) argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
