#include <stdio.h>

/**
 * main - Entry point
 *
 * variables : n as int
 * Description : ptints 0- 9 with comma and space
 * Return : always 0
 */
int main(void)
{
	if (a > b)
		printf("i ambigger");



        if (a > b)
	{
		printf(" i am bigger");
	}

	if (a > b)
		return (0);

	int i;//initialize
	int a, b;//intialize
	i = 5;//declare
	for (int i ;i < b ; i++)
		printf("%s\n", i)

	for (i = 70; i > 45;  --i)
	{
		putchar(i); /*block of code [block] */
	}

	int z;

	z= 60;
	while (z > 50)
	{
		putchar(z);
		--z;
	}

result: 59 58 57 ......51 

	int y;
	y = 60;

	do {
		putchar(y);
		y++;
	} while (y < 80)




      
